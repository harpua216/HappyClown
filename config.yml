base_model: /home/bumpy/qwen3.5-35B-A3B-abliterated
model_type: AutoModelForCausalLM
tokenizer_type: AutoTokenizer
trust_remote_code: true

load_in_8bit: false
load_in_4bit: true

adapter: qlora
lora_r: 64
lora_alpha: 128
lora_dropout: 0.05
lora_target_linear: true

datasets:
  - path: /home/bumpy/datasets/train.jsonl
    type: sharegpt

dataset_prepared_path: /home/bumpy/prepared_data
val_set_size: 0.05

sequence_len: 4096
sample_packing: true
pad_to_sequence_len: true

wandb_project:
wandb_entity:
wandb_watch:
wandb_name:
wandb_log_model:

output_dir: /home/bumpy/output/qwen3.5-35B-A3B-qlora

gradient_accumulation_steps: 4
micro_batch_size: 2
num_epochs: 3
learning_rate: 2e-4
optimizer: paged_adamw_8bit
lr_scheduler: cosine
warmup_steps: 100

bf16: auto
tf32: true

gradient_checkpointing: true
gradient_checkpointing_kwargs:
  use_reentrant: false

deepspeed: deepspeed_zero3.json

logging_steps: 1
save_strategy: steps
save_steps: 500
eval_steps: 500
